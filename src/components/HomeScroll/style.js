import { Box } from "@mui/material";
import styled from "@emotion/styled";

export const HomeScrollBox = styled(Box)`
  display: flex;
  overflow-x: auto;
  white-space: nowrap;
  p: 2;
  max-width: 100%;
  box-sizing: border-box;
`;
